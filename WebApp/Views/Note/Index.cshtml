@model IEnumerable<WebApp.Models.NoteModel>

@{
    ViewBag.Title = "Index";
}

<h2> Notes </h2>

<a href="/Note/Add" class="btn btn-success"> Create note </a>

<ul>
    @foreach (var note in Model)
    {
        <li>@note.Text (@note.CreationDate)</li>
    }
</ul>